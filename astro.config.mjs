import { defineConfig } from 'astro/config';

// If deploying to GitHub Pages without a custom domain, uncomment and set these:
// site: 'https://arjpeg.github.io',
// base: '/6901website',

export default defineConfig({
  output: 'static',
});
